/**
 * @file Admin Listing Management Test Suite
 * @description End-to-end tests for listing creation and management functionality
 * @jest-environment node
 */

const { describe } = require('@jest/globals');

describe('Admin Listing Management', () => {
  // This suite file organizes all listing management tests
  // Individual test files will be loaded automatically by Jest
  
  // Tests included in this suite:
  // - listings.creation.test.js - Tests the complete listing creation flow
  // - listings.management.test.js - Tests listing management operations
  // - listings.filtering.test.js - Tests searching and filtering operations
  // - listings.validation.test.js - Tests form validation during listing creation/editing
  
  // Note: Each test file is fully self-contained with its own setup/teardown
  // to ensure test isolation and reliability.
});