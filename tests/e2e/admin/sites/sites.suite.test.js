/**
 * @file Admin Site Management Test Suite
 * @description End-to-end tests for site creation and management functionality
 * @jest-environment node
 */

const { describe } = require('@jest/globals');

describe('Admin Site Management', () => {
  // This suite file organizes all site management tests
  // Individual test files will be loaded automatically by Jest
  
  // Tests included in this suite:
  // - sites.creation.test.js - Tests the complete site creation flow
  // - sites.management.test.js - Tests site management operations
  // - sites.validation.test.js - Tests form validation during site creation/editing
  
  // Note: Each test file is fully self-contained with its own setup/teardown
  // to ensure test isolation and reliability.
});